# Taste
- Strongly prefers using free / no-cost LLM models (e.g. free OpenRouter models) and does not want paid model usage that spends their money ("stop using my money"). Expects the agent to stay on free models unless explicitly told otherwise. Confidence: 0.9
- Wants research parallelized across subagents rather than done serially, and explicitly encourages delegating high-level tasks (e.g. verification/acceptance checks) to subagents while keeping the main agent on work it already holds context for. Confidence: 0.8
- Wants generated artifacts (e.g. HTML5 games) runnable and verifiable in the browser, and expects a working localhost/browser link to play/demo the result — and the link must actually open (full browser URL, not a stale/relative path). Output that is visibly broken or feels broken when played is unacceptable; expects it to be playtested before being handed over. Confidence: 0.8
- Expects the agent to keep going on its own: when blocked (missing key, failed subagent, API/rate-limit error) it wants the agent to unblock itself and fix things autonomously rather than pausing to ask for direction. Confidence: 0.65
- Values speed over long timelines — expects results in hours, not weeks. Confidence: 0.7
- Repeatedly asks for a concise project/repository overview before diving in (e.g. "overview docs quickly"), especially when returning to stale code — wants the quick-overview pass for docs as well as code. Confidence: 0.7
- Aims to build immersive HTML5 experiences with the highest-grade gamification and game feel (juice, polish), and rejects low-quality/flat output as not good enough. Confidence: 0.7
- Wants documentation to be citation-grounded: every non-obvious claim must cite its source (file path; path + commit SHA for historical claims). Confidence: 0.95
- Never present guesses as facts: inferences that can't be verified from the tree must be explicitly marked UNKNOWN, and the agent should report the count/topics of UNKNOWNs plus anything it couldn't understand. Confidence: 0.95
- Wants docs terse and structured: one concern per file, a machine-readable index (e.g. index.yaml), no prose padding, no restating code in English line-by-line. Confidence: 0.9
- Wants a per-project knowledge base (index, architecture, modules, agents, gates, conventions, decisions, state, glossary) so any agent or human can get productive without re-reading the repo. Confidence: 0.85
- When surveying/auditing a repo, do not modify any existing file — only create new files in a designated area. Confidence: 0.9
- Expects a self-verification pass after producing docs/artifacts: re-read the output against the source and fix anything that cannot be traced. Confidence: 0.85
- Prescribes a survey read order: repo structure first, then every source module, then docs, then tests, then full git history (branches + merges) to reconstruct how and why the project evolved; decision logs should say what changed, when (SHA), and the apparent reason. Confidence: 0.8
- Frames tasks with explicit Objective / Task / Acceptance sections; expects deliverables checked against the stated acceptance criteria and results reported (e.g. file tree, counts). Honors the user's hard scope fences exactly (e.g. "do not change anything outside X", "no other modifications") — zero out-of-scope edits — and when acceptance requires it, reports every file and line changed. Confidence: 0.75
- Dev environment is a Windows host with Docker inside WSL Ubuntu — reach Docker via `wsl -d Ubuntu -- ...`; projects live under C:\dev. Deep quoting through the Windows→WSL→container chain breaks easily, so write a shell script to the scratchpad and pipe it in (`bash < script.sh`) instead of inline one-liners. Confidence: 0.85
- Runs a separate Docker-contained agent ("Hermes", container `hermes-gateway`, control harness at C:\dev\hermes-control) that accumulates skills in SKILL.md frontmatter format at /home/harness/.hermes/skills; expects accumulated skills from prior agent setups to be triaged and ported into Command Code per project rather than starting from scratch. Confidence: 0.8
- Wants a curated triage (not a bulk copy) when migrating prior artifacts/skills: per-item recommendation — take as-is / take with caveat / skip — mapped against the current project's actual needs. Confidence: 0.75
- Treats log/journal files (e.g. HERMES_CRON_LOG.md and any append-only log) as strictly append-only: never prepend or rewrite them from memory of a previous partial read — always re-read the complete current file from git (`git show HEAD:file`) immediately before writing, and never use override/force flags to commit changes that shrink a log file. Confidence: 0.95
- When creating agent-facing rules/memory files, name them `AGENTS.md` at the repo root — Command Code loads `AGENTS.md`, not `CLAUDE.md`; the user verifies that rules actually get picked up by the tool (they asked "do you use claude.md?" and had the ntfy rule file renamed from CLAUDE.md to AGENTS.md so it applies to every agent in the repo). Confidence: 0.85
- Wants agents to push-notify their phone via ntfy when they finish a task, a long-running run completes/fails, or they are blocked waiting on the user's decision — codified as a mandatory rule in the repo's agent rules file (`AGENTS.md` at repo root). Established mechanism: `curl -s --max-time 10 -d "<summary>" https://ntfy.sh/$NTFY_TOPIC`; keep the topic in env (never hardcode it — ntfy topics are unauthenticated, so the topic string is effectively the secret), fire-and-forget so a failed notification never fails the task, and at most one notification per user-visible milestone. User confirmed delivery end-to-end (received the test push on their phone), so this channel is live and in active use. Confidence: 0.95
- When wiring up a new integration or notification channel, wants a live end-to-end test fired immediately (e.g. "wire to <topic> and send a test message") to confirm real delivery, rather than treating the setup as done on config alone (reconfirmed when wiring the GitHub "Wire" channel: acceptance explicitly required "send exactly one test notification and confirm delivery"). Confidence: 0.75
- Wants agent-to-channel communications terse and factual only: a one-line ack when claiming an instruction, then a result post (what changed, files, SHAs, how verified) — no status spam, no progress noise. Confidence: 0.9
- Coordinates long-running agent work through a persistent GitHub issue ("The Wire", talyaak/gd-pipeline #2) between Tal (owner, final authority) and an external coordinator ("Instinct", posts as talyaak-headless with [INSTINCT] prefix); the agent's own prefix is [CMDC], [CLAUDE] comments are retired/historical-only. Expects the agent to re-read the Wire at every session start, treat [INSTINCT] instructions as durable/open until completed or explicitly cancelled/superseded (they survive across sessions), track them in a ledger file (.commandcode/wire-ledger.md: instruction, source comment date, status), treat the user's comments as outranking everything (on conflict, follow the user and note it on the Wire), and keep ntfy as the only channel to the user. Confidence: 0.85
- For GitHub authentication, prefers the `gh` CLI device/web flow run by the agent with the user approving in their own browser ("u should auth via gh and ill approve it") — e.g. `gh auth login --hostname github.com --git-protocol https --web --scopes repo` — instead of asking the user to provision or paste tokens. If the user is away or a device code lapses, don't keep polling in chat: push the approval URL + one-time code to their phone via ntfy (per the standing ntfy rule) — "if you need auth send me ntfy". This route worked end-to-end (code pushed, user approved from phone, gh authenticated as talyaak); device codes expire in minutes, so the push should carry the code plus an "act now" prompt and mention a fresh code will be sent if it lapses. Confidence: 0.8
- Work-loop verification standard: "Never report done what you did not run" — every claim in a result/report must be backed by evidence (SHAs, files changed, commands run and their observed output). The agent is expected to genuinely verify before reporting success (compile/syntax checks, unit checks on routing logic, live API smoke tests, offline harnesses with injected fakes when system deps like chromium/sudo are unavailable) and to state explicitly in the report what was NOT verified. Confidence: 0.9
- Wants small commits with conventional-style subjects, pushed to the agent's working branch rather than merged directly (branch-based flow; merges are gated on the external coordinator's review per the Wire protocol). Confidence: 0.85
